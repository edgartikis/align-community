import { membersStore, readJson } from "./_member-store.mjs";
const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
export default async(request)=>{try{const token=new URL(request.url).searchParams.get("token");if(!token||!/^[A-Za-z0-9_-]{20,}$/.test(token))return json({error:"Tarjeta no encontrada."},404);const member=await readJson(membersStore(),token);if(!member)return json({error:"Tarjeta no encontrada."},404);return json({active:member.status==="Activa",name:member.name,level:member.level,planKey:member.planKey,memberCode:member.memberCode,joinedAt:member.joinedAt,savings:Number(member.savings||0),photoUrl:member.photoUrl||"",needsPhoto:!member.photoUrl});}catch(error){console.error("member-card",error);return json({error:"No pudimos cargar esta tarjeta."},500);}};
