import { groupsStore, membersStore, readJson } from "./_member-store.mjs";
const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
const publicCard=(member)=>({token:member.token,name:member.name,level:member.level,planKey:member.planKey,memberCode:member.memberCode,status:member.status,position:member.position,photoUrl:member.photoUrl||"",savings:Number(member.savings||0)});
export default async(request)=>{try{const token=new URL(request.url).searchParams.get("token");if(!token||!/^[A-Za-z0-9_-]{20,}$/.test(token))return json({error:"Cuenta no válida."},400);const requesting=await readJson(membersStore(),token);if(!requesting||requesting.status!=="Activa")return json({error:"Membresía no encontrada."},404);const group=await readJson(groupsStore(),requesting.groupId);if(!group)return json({primary:publicCard(requesting),cards:[publicCard(requesting)]});if(token!==group.primaryToken)return json({primary:publicCard(requesting),cards:[publicCard(requesting)]});const cards=[];for(const memberToken of group.tokens){const member=await readJson(membersStore(),memberToken);if(member)cards.push(publicCard(member));}return json({primary:cards.find(card=>card.token===group.primaryToken)||cards[0],cards});}catch(error){console.error("group-cards",error);return json({error:"No pudimos cargar las tarjetas."},500);}};
