# DEPRECATED — no usar en producción

Este directorio pertenece a una versión anterior del backend de ALIGN.

No agregar funciones nuevas aquí y no crear endpoints/webhooks que apunten a Netlify.

Backend operativo actual:

`cloudflare/payments-worker/`

API oficial:

`https://api.alignmembers.com.mx`

El contenido se conserva temporalmente únicamente como referencia hasta terminar la certificación de Stripe TEST. Git mantiene el historial, por lo que este directorio puede eliminarse después de esa prueba sin perder versiones anteriores.
