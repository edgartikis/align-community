# DEPRECATED — prototipo anterior

Las Cloudflare Pages Functions de este directorio corresponden a una migración anterior basada en D1.

No son el backend operativo de `api.alignmembers.com.mx` y no deben recibir nuevas funciones.

Backend canónico:

`cloudflare/payments-worker/`

Fuente operativa actual:

Cloudflare KV `PAYMENT_STATE`.

Este código se conserva temporalmente como referencia hasta completar la prueba integral de Stripe TEST.
